/**
 * This package should contain all DTOs which represent entity as it is in database.
 *
 * Such DTOs should be returned by methods which create new entities.
 * Thees DTOs ensure that any other part of program does not modify real entity,
 * without using service which manages the entity's business logic.
 */
package com.netcracker.ncstore.dto.data;